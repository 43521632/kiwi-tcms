# local_settings.py
# 1. Настройки для корректной работы статичных файлов (ВАЖНО!)
#    Эти настройки нужны, даже если вы не используете reverse proxy
STATIC_URL = '/static/'
MEDIA_URL = '/uploads/'

# 1. РЕШЕНИЕ ПРОБЛЕМЫ CSRF (то, что вам нужно для входа)
# Указываем адреса, с которых разрешены запросы
CSRF_TRUSTED_ORIGINS = [
    "http://localhost",
    "http://127.0.0.1",
    # Если используете IP-адрес или домен - добавьте их сюда
    # "http://192.168.1.100",
]

# Для корректной работы за прокси (если используется)
# SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')

# 2. ВАШИ СУЩЕСТВУЮЩИЕ НАСТРОЙКИ (они остаются)
# Отключение требования сложного пароля для локальной разработки
AUTH_PASSWORD_VALIDATORS = []

# Разрешить регистрацию новым пользователям
ACCOUNT_ALLOW_REGISTRATION = True

# 3. ДОПОЛНИТЕЛЬНЫЕ ПОЛЕЗНЫЕ НАСТРОЙКИ ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ
# Отключаем проверку HTTPS (для локального HTTP)
# KIWI_DONT_ENFORCE_HTTPS = True  # Раскомментируйте, если нужно

# Включаем debug mode для детальных логов ошибок
# DEBUG = True  # Раскомментируйте при необходимости, но выключите в продакшене
